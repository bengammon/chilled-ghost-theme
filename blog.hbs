{{!< default}}

{{#get "posts" include="tags" filter="tags:-note" limit="all"}}

    <h1>Blog</h1>

    {{> tags}}

    <div class="archive">
        {{#get "posts" include="tags" filter="tags:-note" limit="all"}}
            {{#foreach posts}}
                {{> post-teaser}}
            {{/foreach}}
        {{/get}}
    </div>

{{/get}}