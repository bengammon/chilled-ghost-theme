<div class="tags">
	{{#get "tags" limit="all" filter="slug:-micro"}}
		{{#foreach tags}}
		<a class="tags__item" href="{{url}}" title="{{name}}" {{#match slug "=" ../../currentTag}}aria-current="page"{{/match}}>#{{name}}</a>
		{{/foreach}}
	{{/get}}
</div>
