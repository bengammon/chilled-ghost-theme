<nav class="nav-container">
    <ul class="nav">
        {{#foreach @site.navigation}}
        <li class="nav__item">
            <a href="{{this.url}}">{{label}}</a>
        </li>
        {{/foreach}}
    </ul>
</nav>
