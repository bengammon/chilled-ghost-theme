{{!< default}}

<h1>Notes {{#if pagination.prev}}<span class="color--text-subtle">(Page {{pagination.page}})</span>{{/if}}</h1>

{{> tags}}

<div class="archive">
    {{#foreach posts}}
        {{> post-note}}
    {{/foreach}}
</div>

{{> pagination}}